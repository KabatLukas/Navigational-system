<UserControl x:Class="Navigacny_system.Gui.LineChartAvg"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Navigacny_system.Gui"
             xmlns:wpf="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" >
    <Grid x:Name="Grid1">
        <!--<wpf:CartesianChart x:Name="AvgCartesianChart" Series="{Binding SeriesCollection}" LegendLocation="None" DisableAnimations="True" Hoverable="False" DataTooltip="{x:Null}">
            <wpf:CartesianChart.AxisY>
                <wpf:Axis x:Name="AxisY" Title="Average time" MinValue="{Binding Path=MinY ,Mode = OneWay}" MaxValue="{Binding Path=MaxY ,Mode = OneWay}"/>
            </wpf:CartesianChart.AxisY>
            <wpf:CartesianChart.AxisX>
                <wpf:Axis Title="Replication" Labels="{Binding Labels}"></wpf:Axis>
            </wpf:CartesianChart.AxisX>
        </wpf:CartesianChart>-->
    </Grid>
</UserControl>
